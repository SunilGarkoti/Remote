
pipeline { 
    agent any 
    
    stages {
        stage('Build') { 
            steps { 
                echo "Build is running" 
            }
        }
        stage('Test'){
          environment { 
                AN_ACCESS_KEY = credentials('git_username_password') 
            }
          
          steps {
                echo "Test is running"
                
                build 'Maven_Project'
            }
              
        }
        stage('Deploy') {
            steps {
                echo "Deployement is on processs"
                 sh "/Users/sunilgarkoti/pipeline/make.sh"
            }
        }
    }
}
